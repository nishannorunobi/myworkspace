# Dashboard Agent — Server Configuration
# Edit this file to change server behaviour. Restart required for changes to take effect.

HOST=0.0.0.0
PORT=8888

STATIC_DIR=static
LOG_DIR=logs
LOG_LEVEL=info
