#!/bin/bash
# project.conf — project-specific configuration.
# Sourced by dockerspace/functions.sh (host and container).

# ─── Project name ─────────────────────────────────────────────────────────────

# Drives IMAGE_NAME and CONTAINER_NAME in workspace.conf
PROJECT_NAME="myapigw"

# ─── Git identity ─────────────────────────────────────────────────────────────

GIT_USER_NAME="Nishan"
GIT_USER_EMAIL="norunnabinishan@gmail.com"

# ─── Project git clone ────────────────────────────────────────────────────────

# Cloned automatically into projectspace/ after container setup.
# Leave GIT_CLONE_URL empty to skip cloning.
#GIT_CLONE_URL="git@github.com:nishannorunobi/ums.git"
GIT_CLONE_URL="git@github.com:nishannorunobi/myapigw.git"
# ─── Project-specific tools — uncomment and set as needed ─────────────────────

# PYTHON_VERSION="3.10"
# FFMPEG_VERSION="latest"

# ─── File transfer paths ──────────────────────────────────────────────────────

# Input file name to copy from Downloads (leave empty to specify via command line)
INPUT_FILE="amaro_porano_jaha_chay.mp4"

# Destination directory constructed in script: $WORKSPACE_ROOT/$MOUNTSPACE_DIR (from workspace.conf)
SRC_DIR="/home/nishan/Downloads/"
